import NavBar from "../components/NavBar"
//import { APIDataContext } from "../App"
//import { useContext } from "react"

const Players = () => {

    //const {players} = useContext(APIDataContext);
    //console.log(players);


    return (
        <NavBar></NavBar>
    )
}
export default Players